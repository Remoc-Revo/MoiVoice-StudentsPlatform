import 'package:flutter/material.dart';
import 'package:moivoice/provider/auth_provider.dart' as auth;

var backgroundColor = Colors.white;

//colors for both text and icon when the user turns to anonymous mode
var TextIconColor = Colors.black;

//var userName = AuthProvider.userName;
//var userEmail = auth.AuthProvider.userEmail;
